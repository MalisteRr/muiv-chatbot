# 🎓 Чат-бот для абитуриентов МУИВ

> **Дипломный проект**  
> **Автор:** Синицин Михаил Дмитриевич  
> **Университет:** МУИВ (Московский Университет им С.Ю. Витте)  
> **Направление:** 09.03.03 Прикладная информатика (Искусственный интеллект)

---

## 📋 О проекте

Интеллектуальный Telegram-бот для автоматизации консультирования абитуриентов. Использует технологии искусственного интеллекта (LLM) и машинного обучения для обработки запросов на естественном языке.

### ✨ Ключевые возможности

- 🤖 **AI-обработка запросов** - понимание вопросов на естественном языке (LLM)
- 📚 **База знаний** - структурированная информация о поступлении
- 🔍 **Умный поиск** - контекстный поиск по FAQ
- 💬 **Контекстные диалоги** - запоминание истории разговора
- 📊 **Аналитика** - детальная статистика использования
- 👥 **Админ-панель** - управление и мониторинг

---

## 🚀 БЫСТРЫЙ СТАРТ (для защиты диплома)

### ⚡ Вариант 1: SQLite (РЕКОМЕНДУЕТСЯ для защиты)

**Преимущества:**
- ✅ Не требует установки PostgreSQL
- ✅ Работает сразу "из коробки"
- ✅ Идеально для локальной демонстрации
- ✅ Вся БД в одном файле `muiv_bot.db`

**Шаги:**

```bash
# 1. Скачать проект
git clone https://github.com/malisterr/muiv-chatbot.git
cd muiv-chatbot

# 2. Создать виртуальное окружение
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Настроить .env файл
copy .env.example .env  # Windows
cp .env.example .env    # Linux/Mac

# Открыть .env и указать:
# - TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
# - DATABASE_URL=sqlite:///muiv_bot.db
# - OPENAI_API_KEY=ваш_ключ_API

# 5. Запустить бота
python main.py
```

**Готово! Бот запущен и работает! 🎉**

---

### 🐘 Вариант 2: PostgreSQL (для продакшена)

```bash
# Дополнительно нужно:
# 1. Установить PostgreSQL
# 2. Создать базу данных

# Windows (через официальный установщик PostgreSQL):
# Скачать с https://www.postgresql.org/download/windows/

# Linux:
sudo apt install postgresql postgresql-contrib

# 3. Создать БД
sudo -u postgres psql
CREATE DATABASE muiv_bot;
CREATE USER muiv_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE muiv_bot TO muiv_user;
\q

# 4. В .env указать:
DATABASE_URL=postgresql://muiv_user:your_password@localhost:5432/muiv_bot

# 5. Запуск
python main.py
```

---

## 📁 Структура проекта

```
muiv-chatbot/
│
├── main.py                      # 🚀 Точка входа (ЗАПУСКАТЬ)
├── config.py                    # ⚙️ Конфигурация
├── requirements.txt             # 📦 Зависимости
├── .env                         # 🔐 Переменные окружения (создать!)
├── .env.example                 # 📋 Пример .env
├── .gitignore                   # 🚫 Игнорируемые файлы
├── README.md                    # 📖 Документация
│
├── bot/                         # Модуль Telegram бота
│   ├── dispatcher.py            # Инициализация бота
│   ├── keyboards.py             # Клавиатуры
│   └── handlers/                # Обработчики
│       ├── common.py            # /start, /help, /contacts
│       ├── user.py              # Обработка вопросов
│       └── admin.py             # Админ-панель
│
├── ml/                          # ML компоненты
│   └── chat_manager.py          # Менеджер диалогов + AI
│
├── database/                    # Работа с БД
│   ├── init_db.py               # Инициализация БД
│   └── crud.py                  # CRUD операции
│
├── utils/                       # Утилиты
│   ├── logger.py                # Логирование
│   ├── helpers.py               # Вспомогательные функции
│   └── text_processing.py       # NLP обработка
│
├── data/                        # 📊 Данные
│   └── faq_sample.json          # Пример FAQ
│
└── logs/                        # 📝 Логи (создаются автоматически)
```

---

## 🔧 Конфигурация (.env файл)

Создайте файл `.env` в корне проекта:

```env
# ============ TELEGRAM BOT ============
# Получить токен: https://t.me/BotFather
TELEGRAM_BOT_TOKEN=123123123123:AAFjUqrYN2bktrEtip01zV1e-fxd4UyN8tY

# ID администраторов (свой ID узнать: https://t.me/userinfobot)
ADMIN_IDS=1231231231313

# ============ DATABASE ============
# Для SQLite (РЕКОМЕНДУЕТСЯ для демонстрации):
DATABASE_URL=sqlite:///muiv_bot.db

# Для PostgreSQL:
# DATABASE_URL=postgresql://muiv_user:your_password@localhost:5432/muiv_bot

# ============ AI MODEL ============
# OpenRouter API (или OpenAI)
OPENAI_API_KEY=sk-or-v1-ваш-ключ-здесь

# API endpoint
OPENAI_BASE_URL=https://openrouter.ai/api/v1

# Модель (рекомендуем deepseek - быстрая и дешевая)
LLM_MODEL=deepseek/deepseek-r1

# Параметры
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=600

# ============ ПРИЛОЖЕНИЕ ============
DEBUG=false
```

---

## 🎯 Как получить API ключи

### 1. Telegram Bot Token

1. Открыть [@BotFather](https://t.me/BotFather) в Telegram
2. Отправить `/newbot`
3. Указать имя бота (например: "Бот поддержки")
4. Указать username (например: `abiturient_bot`)
5. Скопировать токен из ответа

### 2. OpenRouter API Key (для AI)

1. Зарегистрироваться на [openrouter.ai](https://openrouter.ai)
2. Перейти в раздел "API Keys"
3. Создать новый ключ
4. Пополнить баланс (минимум $5)

**Альтернатива:** Можно использовать OpenAI напрямую:
```env
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-proj-...
LLM_MODEL=gpt-3.5-turbo
```

### 3. Admin ID (свой Telegram ID)

1. Написать боту [@userinfobot](https://t.me/userinfobot)
2. Скопировать свой ID из ответа

---

## 📊 База данных

### Автоматическое создание таблиц

При первом запуске бот автоматически создаст все необходимые таблицы:

- `users` - пользователи бота
- `faq` - база знаний (вопросы-ответы)
- `chat_history` - история диалогов
- `analytics` - аналитика запросов
- `user_stats` - статистика пользователей
- `feedback` - обратная связь
- `faq_categories` - категории FAQ
- `admin_logs` - логи администратора
- `broadcasts` - рассылки

### Загрузка тестовых данных

```bash
# Создать файл data/faq_sample.json с примерами вопросов
# Затем загрузить его:
python scripts/load_faq.py data/faq_sample.json
```

---

## 🤖 Использование бота

### Для пользователей

**Команды:**
- `/start` - начать диалог
- `/help` - справка
- `/clear` - очистить историю
- `/stats` - моя статистика
- `/contacts` - контакты МУИВ

**Как задавать вопросы:**
- Можно использовать кнопки меню
- Или писать свободным текстом на естественном языке

**Примеры вопросов:**
- "Какие документы нужны для поступления?"
- "Сколько стоит обучение на юриста?"
- "Есть ли бюджетные места на IT?"
- "Можно ли поступить без ЕГЭ?"

### Для администраторов

**Админ-команды:**
- `/admin` - открыть админ-панель
- `/stats_full` - полная статистика
- `/analytics` - аналитика за период
- `/popular` - топ вопросов
- `/unanswered` - вопросы без ответов
- `/users` - список пользователей
- `/export` - экспорт данных
- `/reload_kb` - перезагрузить базу знаний

---

## 📈 Мониторинг

### Логи

Логи сохраняются в папке `logs/`:
- `muiv_bot_YYYYMMDD.log` - общие логи
- `muiv_bot_errors_YYYYMMDD.log` - только ошибки

### Просмотр логов в реальном времени

```bash
# Windows
type logs\muiv_bot_20250118.log

# Linux/Mac
tail -f logs/muiv_bot_20250118.log
```

---

## 🐛 Решение проблем

### Проблема: "TELEGRAM_BOT_TOKEN не установлен"

**Решение:**
1. Убедитесь что файл `.env` создан в корневой папке
2. Проверьте что токен указан правильно (без пробелов)
3. Перезапустите бота

### Проблема: "Ошибка подключения к БД"

**Решение для SQLite:**
```env
DATABASE_URL=sqlite:///muiv_bot.db
```

**Решение для PostgreSQL:**
1. Убедитесь что PostgreSQL запущен
2. Проверьте правильность DATABASE_URL
3. Создайте базу данных вручную

### Проблема: "OpenAI API error"

**Решение:**
1. Проверьте API ключ
2. Убедитесь что есть средства на балансе
3. Проверьте что model доступна

### Проблема: "ModuleNotFoundError"

**Решение:**
```bash
pip install -r requirements.txt --upgrade
```

---

## 🌐 Хостинг (для постоянной работы)

### Бесплатные варианты

#### 1. Railway.app (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Зарегистрироваться на railway.app
# 2. Создать новый проект
# 3. Подключить GitHub репозиторий
# 4. Добавить переменные окружения в настройках
# 5. Railway автоматически развернет бота
```

#### 2. Render.com

```bash
# 1. Создать аккаунт на render.com
# 2. New > Web Service
# 3. Подключить репозиторий
# 4. Настроить переменные окружения
# 5. Deploy
```

#### 3. Fly.io

```bash
# Установить CLI
curl -L https://fly.io/install.sh | sh

# Войти
flyctl auth login

# Запустить
flyctl launch

# Установить переменные
flyctl secrets set TELEGRAM_BOT_TOKEN=...
```

### VPS/Dedicated Server

```bash
# 1. Подключиться к серверу
ssh user@your-server.com

# 2. Установить зависимости
sudo apt update
sudo apt install python3.10 python3-pip git

# 3. Клонировать проект
git clone https://github.com/malisterr/muiv-chatbot.git
cd muiv-chatbot

# 4. Настроить виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 5. Настроить .env

# 6. Создать systemd service
sudo nano /etc/systemd/system/muiv-bot.service

[Unit]
Description=MUIV Abiturient Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/home/youruser/muiv-chatbot
Environment="PATH=/home/youruser/muiv-chatbot/venv/bin"
ExecStart=/home/youruser/muiv-chatbot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# 7. Запустить
sudo systemctl daemon-reload
sudo systemctl enable muiv-bot
sudo systemctl start muiv-bot

# Проверить статус
sudo systemctl status muiv-bot

# Посмотреть логи
sudo journalctl -u muiv-bot -f
```

---

## 🧪 Тестирование

```bash
# Запустить тесты
pytest

# С покрытием кода
pytest --cov=. --cov-report=html

# Открыть отчет о покрытии
# Windows: start htmlcov/index.html
# Linux: xdg-open htmlcov/index.html
```

---

## 📝 Чеклист для защиты

- [ ] Код скачан с GitHub
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` настроен с вашими ключами
- [ ] Бот успешно запускается (`python main.py`)
- [ ] Бот отвечает на команду `/start`
- [ ] Бот отвечает на вопросы через AI
- [ ] Админ-панель работает (`/admin`)
- [ ] Логи пишутся в папку `logs/`
- [ ] База данных создается автоматически
- [ ] Презентация готова
- [ ] Демонстрация отрепетирована

---

## 🎓 Технологический стек

- **Python 3.10+** - язык программирования
- **aiogram 3.x** - фреймворк для Telegram ботов
- **SQLite / PostgreSQL** - база данных
- **OpenAI API / OpenRouter** - языковые модели (LLM)
- **asyncio** - асинхронное программирование
- **asyncpg / aiosqlite** - асинхронные драйверы БД

---

## 📞 Поддержка

**Разработчик:** Синицин Михаил Дмитриевич  
**GitHub:** [github.com/malisterr](https://github.com/malisterr)  
**Email:** (sinicinday@gmail.com)

**МУИВ:**  
📞 8 (800) 550-03-63  
✉️ pk@muiv.ru  
🌐 muiv.ru

---

## 📄 Лицензия

Разработано в рамках дипломного проекта МУИВ.

© 2025 Синицин Михаил Дмитриевич

---

**Последнее обновление:** Декабрь 2025

